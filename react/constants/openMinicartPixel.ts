/**
 * Debe coincidir con minicart.v2 → props.customPixelEventId del theme.
 * Con el patrón típico "add-to-cart-button" el minicart abre con el mismo
 * evento que emite el add-to-cart: { id, event: 'addToCart', items }.
 */
export const OPEN_MINICART_PIXEL_ID = 'add-to-cart-button'
